/**
 * Hero image path — used only by HeroSection (video poster / mobile fallback).
 */

export const IMAGE_PLACEHOLDER = "/images/placeholder.svg";

export const heroImages = {
  factory: "/images/hero/hero-factory.jpg",
} as const;
